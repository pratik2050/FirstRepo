#include<stdio.h>
using namespace std
int main()
{
  int a, b;
  printf("enter the first number ");
  scanf(%d, &a);
  printf("enter the second number ");
  scanf(%d, &b);
  int c;
  c = a + b;
  printf(c);
  }
  
